[project]
name = "estimator-ui"
version = "0.1.0"
description = "Streamlit chat UI for the estimator service"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.40",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]
